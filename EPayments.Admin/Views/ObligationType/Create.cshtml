@model EPayments.Admin.Models.ObligationType.ObligationTypeVM

<div class="container main-content obligation-create">

    <h1 style="text-align: center;">Добавяне на ново тип задължение</h1>

    @using (Html.BeginForm(MVC.ObligationType.ActionNames.Create, MVC.ObligationType.Name, null, FormMethod.Post, new { @class = "clearfix", style = "display: block;" }))
    {
        if (!ViewData.ModelState.IsValid)
        {
            @Html.ValidationSummary()
            <br />
        }

        <div class="row">
            @Html.Partial(MVC.ObligationType.Views.Form, Model)
        </div>

        <div class="row">
            <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12 pull-right">
                <div class="form-group">
                    <label>&nbsp;</label>
                    <input class="btn blue-btn" type="submit" value="Запиши" style="width: 100%; padding: 5px 0 5px 0;">
                </div>
            </div>

            <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12 pull-right">
                <div class="form-group">
                    <label>&nbsp;</label>
                    <a style="width: 100%; padding: 5px 0 5px 0; text-align: center;" class="btn grey-btn"
                       href="@Url.Action(MVC.ObligationType.ActionNames.List, MVC.ObligationType.Name)">Отказ</a>
                </div>
            </div>
        </div>
    }

</div>